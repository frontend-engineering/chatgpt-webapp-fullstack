{
    "builds": [
        {
            "src": "/node-api/bin/server.js",
            "use": "@vercel/node"
        }
    ],
    "rewrites": [
        {
            "source": "/(.*)",
            "destination": "/node-api/bin/server.js"
        }
    ]
}
